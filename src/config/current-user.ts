/**
 * Placeholder identity.
 *
 * TODO: replace with the signed-in user once auth lands. Kept in one module so
 * that swap is a single edit rather than a search across every greeting.
 */
export const CURRENT_USER = { firstName: "Shivansh" };
